# Same-origin in dev (Vite proxy) and in production (API container serves the SPA).
# Only override if the API is ever hosted separately — that also requires CORS.
VITE_API_BASE_URL=/api/v1

# Where the Vite dev proxy forwards /api. Not used in production builds.
VITE_DEV_API_TARGET=http://localhost:8000
